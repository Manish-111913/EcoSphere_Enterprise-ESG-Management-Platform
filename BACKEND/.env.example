# infra ONLY — business config lives in the database (app_settings), never here
# host port 5433 avoids clashing with a native Postgres often already on 5432
DATABASE_URL=postgresql://postgres:dev@localhost:5433/ecosphere
JWT_SECRET=change-me
PORT=4000
